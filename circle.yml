machine:
  node:
    version: 6.9.1

dependencies:
  override:
    - npm i
    - npm run lint
    - npm run build

test:
  override:
    - npm run test

deployment:
  production:
    branch: master
    commands:
      - ./copy_for_deploy.sh 
